kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: trello
spec:
  host: trello-board.apps.dev02.ocp.dev02
  to:
    kind: Service
    name: projectmgmt-app-emea
    weight: 100
  port:
    targetPort: 3000
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Allow
  wildcardPolicy: None

