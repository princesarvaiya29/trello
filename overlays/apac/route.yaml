kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: trello
spec:
  host: trello-board.apps.dev03.ocp.dev03
  to:
    kind: Service
    name: projectmgmt-app-apac
    weight: 100
  port:
    targetPort: 3000
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Allow
  wildcardPolicy: None